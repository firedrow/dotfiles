Notes about font files.

Make sure to run fc-cache after adding more fonts.

On a new system, run:

* mkfontscale /<font path/>
* mkfontdir /<font_path/>
* Using stow, font path is ~/.local/share/fonts
